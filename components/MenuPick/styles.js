import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    top: -100,
  },
  menuContainer: {
    padding: 60,
    alignSelf: 'stretch',
  },
  headerText: {
    fontSize: 20,
    margin: 10,
    fontWeight: 'bold',
  },
  menuContent: {
    color: '#000',
    fontWeight: 'bold',
    padding: 2,
    fontSize: 16,
  },
});

export default styles;
